[package]
name=parallel
version=1.0.0
include=libparallel
description=Run multiple jobs or functions in parallel with only Bash.

[dependencies]
assign=*

[devDependencies]
embed-docs=*

[scripts]
readme=embed-docs README.md libparallel
